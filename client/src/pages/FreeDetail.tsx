import styled from 'styled-components';

const FDContainer = styled.main`
  background-color: var(--gray);
  color: white;
  display: flex;
  justify-content: center;
  margin-top: 100px;
  height: 100%;
`;

const FreeDetail = () => <FDContainer>FreeDetail</FDContainer>;

export default FreeDetail;
